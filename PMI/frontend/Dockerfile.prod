FROM node:20-alpine

WORKDIR /workspace

# Copy packages
COPY packages ./packages

# Setup frontend
WORKDIR /workspace/PMI/frontend
COPY PMI/frontend/package.json ./

# Adjust paths for Docker
RUN sed -i 's|"file:../../packages|"file:/workspace/packages|g' package.json

RUN npm install

COPY PMI/frontend .

ENV DOCKER_ENV=true

# Build-time environment variables
ARG NEXT_PUBLIC_API_URL=http://localhost:18100
ARG NEXT_PUBLIC_IDENTITY_URL=http://localhost:13110

ENV NEXT_PUBLIC_API_URL=$NEXT_PUBLIC_API_URL
ENV NEXT_PUBLIC_IDENTITY_URL=$NEXT_PUBLIC_IDENTITY_URL

RUN npm run build

EXPOSE 3000

CMD ["npm", "start"]
