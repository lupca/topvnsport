services:
  oms_db:
    image: postgres:15-alpine
    container_name: oms_db
    restart: always
    environment:
      POSTGRES_DB: oms_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "15434:5432"
    volumes:
      - oms_db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 12

  oms_backend:
    build:
      context: ./backend
      dockerfile: Dockerfile.dev
    container_name: oms_backend
    restart: always
    ports:
      - "18101:8001"
    environment:
      - DATABASE_URL=postgresql://postgres:postgres@oms_db:5432/oms_db
      - INTEGRITY_MODE=development
      - ENV=development
      # Development-only key; never reuse this value in production.
      - FERNET_KEY=2Jf7oG7N4zFv2j3GmY5V0rLq9xW8pC1aB6dE3hK7nQw=
      - JWT_SECRET_KEY=${JWT_SECRET_KEY:-identity_jwt_secret_key_2026_change_me_in_prod}
      - SEPAY_MERCHANT_ID=${SEPAY_MERCHANT_ID:-}
      - SEPAY_SECRET_KEY=${SEPAY_SECRET_KEY:-}
      - SEPAY_CHECKOUT_URL=${SEPAY_CHECKOUT_URL:-https://pay.sepay.vn/v1/checkout/init}
      - WEB_BASE_URL=${WEB_BASE_URL:-https://topvnsport.vn}
    depends_on:
      oms_db:
        condition: service_healthy
    networks:
      - default
      - pmi_default
      - wms_default
      - gateway_network
    develop:
      watch:
        - action: sync
          path: ./backend
          target: /app
          ignore:
            - __pycache__/
            - "*.pyc"
        - action: rebuild
          path: ./backend/requirements.txt

  oms_frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.dev
    container_name: oms_frontend
    restart: always
    ports:
      - "13101:3001"
    environment:
      - NEXT_PUBLIC_OMS_API_URL=http://localhost:18101
      - NEXT_TELEMETRY_DISABLED=1
    depends_on:
      - oms_backend
    networks:
      - default
      - pmi_default
      - wms_default
    develop:
      watch:
        - action: sync
          path: ./frontend
          target: /app
          ignore:
            - node_modules/
            - .next/
        - action: rebuild
          path: ./frontend/package.json

networks:
  default:
    name: oms_default
    external: true
  pmi_default:
    external: true
  wms_default:
    external: true
  gateway_network:
    external: true

volumes:
  oms_db_data:
